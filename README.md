# Spinner

Single-file option spinner. Press space / tap to spin, S to edit options, F for full screen, M to mute.

Share links encode the option list in the URL hash, so each creator can get a preset link.

## Deploy
Static file — GitHub Pages, Vercel, Netlify, or any host. No build step.
